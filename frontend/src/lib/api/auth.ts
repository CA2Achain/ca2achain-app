import apiClient from './client'

// TODO: Implement auth API calls
export const authApi = {
  // login: (email: string, accountType: 'buyer' | 'dealer') => Promise
  // verifyMagicLink: (token: string) => Promise
  // logout: () => Promise
}
